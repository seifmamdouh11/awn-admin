import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Withdrawals Desk",
};

export default function WithdrawalsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
